import { {{properCase name}}DTO } from './{{camelCase name}}.ts';

describe('{{properCase name}} Unit Tests', () => {
  it('should create a valid {{camelCase name}}DTO', () => {
    const dto: {{properCase name}}DTO = {
      id: '1',
      name: 'Test Name',
      // Add more properties as needed
    };

    expect(dto).toHaveProperty('id');
    expect(dto.name).toBe('Test Name');
  });

  // Additional unit tests can be added here
});
