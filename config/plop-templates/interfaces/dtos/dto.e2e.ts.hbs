import request from 'supertest';
import { app } from '@@app'; // Update to your app's path
import { {{properCase name}}DTO } from './{{camelCase name}}.ts';

describe('{{properCase name}} Endpoints', () => {
  it('should create a new {{camelCase name}}', async () => {
    const newDto: {{properCase name}}DTO = {
      id: '1',
      name: 'Test Name',
      // Add more properties as needed
    };
    
    const response = await request(app).post('/{{camelCase name}}').send(newDto);
    expect(response.status).toBe(201);
    expect(response.body).toHaveProperty('id');
  });

  // Additional tests can be added here
});
