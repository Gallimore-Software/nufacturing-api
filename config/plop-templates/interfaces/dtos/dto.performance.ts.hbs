import { performance } from 'perf_hooks';
import { {{properCase name}}DTO } from './{{camelCase name}}.ts';

describe('{{properCase name}} Performance Tests', () => {
  it('should create a {{camelCase name}} quickly', () => {
    const start = performance.now();
    const newDto: {{properCase name}}DTO = {
      id: '1',
      name: 'Test Name',
      // Add more properties as needed
    };

    // Simulate creation logic
    // create{{properCase name}}(newDto); // Uncomment when the function is available

    const duration = performance.now() - start;
    expect(duration).toBeLessThan(100); // Adjust the threshold as needed
  });

  // Additional performance tests can be added here
});
